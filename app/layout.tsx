import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "College API - Comprehensive Indian Colleges Database | Next.js REST API",
  description: "Free and open-source REST API providing comprehensive information about colleges across India. Built with Next.js, featuring random college lookup, alphabetical filtering, and complete college listings.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
